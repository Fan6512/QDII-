# 复制本文件为 .env.local 并在本地部署/测试时填；线上在 Vercel 控制台 Project Settings → Environment Variables 设置：
#
# 管理端密钥：任意强随机串，管理端 admin.html 输入与此一致才能确认候选。仅通过 x-admin-key 请求头传递。
ADMIN_KEY=在这里填一个强随机串(建议32位以上，例如 python -c "import secrets;print(secrets.token_urlsafe(32))")
#
# Upstash Redis REST 连接信息。通过 Vercel Storage 创建并连接 Upstash 后通常会自动注入。
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
